console.log("ld main.js");

function makeSweet(name, kind, flavor, topping, topcolor){
    name = name;
    kind = kind;
    flavor = flavor;
    topping = topping;
    topcolor = topcolor;
};

inventory.push(new makeSweet("Christmas Tree", "cake", "white", "none", "green"));